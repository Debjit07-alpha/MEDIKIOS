# OCR.Space API key — used by POST /api/ocr and prescription analysis.
# Get a key at https://ocr.space/ocrapi
# Never commit the real key. Paste it into backend/.env locally and set it
# as an environment variable on Render.
OCR_API_KEY=